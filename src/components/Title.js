import React from 'react';

const Title = props => <div>
                        <h1>{props.title}</h1>
    //                  <p>Tasks: ({})</p> Tutaj ma się wyświetlać liczba zadań
                      </div>

export default Title;
